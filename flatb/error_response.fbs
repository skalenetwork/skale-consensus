namespace skale_fb;

// ErrorResponse remains a table due to variable-length string
table ErrorResponse {
    status: uint;
    substatus: uint;
    last_block: uint64;
    last_block_timestamp_s: uint64;
    last_block_timestamp_ms: uint64;
    message: string ;
}



root_type ErrorResponse;
