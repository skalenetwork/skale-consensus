include "common_structures.fbs";

namespace skale_fb;

table DecryptionShares {
  block_id: uint64;
  proposer_index: uint64;
  decryptor_index: uint64;
  decryption_shares: [DecryptionShare];
  signature: [uint8];
}

root_type DecryptionShares;
